// Feature flags. Flip a value to true to re-enable a hidden feature instantly.
export const features = {
  // Cart drawer (shopping bag icon + checkout) in the site header.
  // Set to true when we're ready to take orders through the cart again.
  cart: false,
} as const;
